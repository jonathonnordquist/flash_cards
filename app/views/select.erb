<div class="container">
  <h1>Select your Deck</h1>
  <ul>
    <% @all_decks.each do |deck| %>
      <li><%= deck.name %><a href = "/game/start/<%= deck.id %>">New Game</a></li>
    <% end %>
  </ul>
</div>
